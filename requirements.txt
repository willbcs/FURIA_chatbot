# Dependências principais (core)
python-telegram-bot==20.8  # Versão estável da série 20.x (compatível com seu código)
selenium==4.19.0           # Suporte ao Chrome mais recente
webdriver-manager==4.0.1   # Gerenciamento automático de drivers

# Web scraping e HTTP
beautifulsoup4==4.12.3     # Parsing HTML robusto
requests==2.31.0           # Cliente HTTP (versão estável)
fake-useragent==1.3.0      # User-agents atualizados

# Gerenciamento de ambiente
python-dotenv==1.0.0       # Variáveis de ambiente

# Web server
Flask==3.0.2               # Versão LTS estável

# Dependências de desempenho (opcionais)
cachetools==5.3.2          # Melhora performance do PTB
httpx==0.27.0              # Cliente HTTP moderno (para futuras atualizações)